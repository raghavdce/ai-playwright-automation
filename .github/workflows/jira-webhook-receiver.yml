name: Jira Webhook Receiver

on:
  workflow_dispatch:
  repository_dispatch:
    types: [jira_webhook]

jobs:
  forward:
    runs-on: ubuntu-latest
    steps:
      - name: Log Jira payload
        run: |
          echo "Payload received"
          echo '${{ toJson(github.event) }}'
